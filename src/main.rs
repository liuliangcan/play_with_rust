mod problem;
#[allow(unused)]
use std::collections::*;
// use std::intrinsics::atomic_load_acquire;
use std::io::{BufRead, BufWriter, Write};

fn main() {
    let mut b = 1;

    println!("{}",b);
}

